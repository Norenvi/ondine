POSTGRES_PASSWORD=changeme
UMAMI_APP_SECRET=changeme
# Umami website ID for this environment (local dev / VM each have their own, since they're
# separate Umami instances/databases). Baked into the frontend at build time. Create the
# website in the Umami dashboard first (see CLAUDE.md), then set this and rebuild caddy.
VITE_UMAMI_WEBSITE_ID=
